// ════════════════════════════════════════════════════════════════════════════
// 📚 QWER NOTES - JAVASCRIPT DOM (COMPLETE REFERENCE)
// ════════════════════════════════════════════════════════════════════════════


// ════════════════════════════════════════════════════════════════════════════
// SECTION 1: DOM BASICS & UNDERSTANDING
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🤔 DOM KYA HAI? (What is DOM?)                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ DOM = Document Object Model                                             │
│                                                                          │
│ Simple Words Me:                                                         │
│ - HTML page ko JavaScript objects ki tarah treat karta hai              │
│ - Har HTML element ek object hai jisko manipulate kar sakte ho         │
│ - Tree structure me organized (parent-child relationship)               │
│                                                                          │
│ Real Life Example:                                                       │
│ HTML = Building Blueprint (Design)                                       │
│ DOM = Actual Building (Jo modify kar sakte ho)                         │
│                                                                          │
│ DOM Tree Structure:                                                      │
│                                                                          │
│        document                                                          │
│           │                                                              │
│        <html>                                                            │
│       /      \                                                           │
│    <head>   <body>                                                       │
│      │      /    \                                                       │
│   <title> <div> <p>                                                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 1.1 Document Object (Entry Point)
// ────────────────────────────────────────────────────────────────────────────

// 'document' = Pura HTML page ko represent karta hai
console.log(document);              // Pura page object
console.log(document.title);        // Page ka title
console.log(document.URL);          // Current page ka URL
console.log(document.domain);       // Domain name


// ════════════════════════════════════════════════════════════════════════════
// SECTION 2: SELECTING ELEMENTS (DOM Elements Ko Pakadna)
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🎯 SELECTION METHODS (Elements Ko Select Karne Ke Tareeqe)             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ Modern (2025 Recommended):                                               │
│ ✅ querySelector()      - First match return (CSS selector)             │
│ ✅ querySelectorAll()   - All matches return (NodeList)                 │
│                                                                          │
│ Old Methods (Still Valid):                                               │
│ ⚠️  getElementById()     - ID se select                                 │
│ ⚠️  getElementsByClassName() - Class se select (HTMLCollection)         │
│ ⚠️  getElementsByTagName()   - Tag se select (HTMLCollection)           │
│                                                                          │
│ Best Practice: querySelector/querySelectorAll use karo (modern & clean) │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 2.1 querySelector() - First Match Return Karta Hai
// ────────────────────────────────────────────────────────────────────────────

// By ID (# sign use karo)
const heading = document.querySelector('#main-heading');
//              └────────┬─────────┘ └──────┬───────┘
//                       │                  └─ CSS selector: #id
//                       └──────────────────── Returns first match or null

// By Class (. sign use karo)
const btn = document.querySelector('.btn-primary');
//                                  └─┬─┘
//                                    └─ CSS selector: .class

// By Tag Name (direct tag name)
const paragraph = document.querySelector('p');  // First <p> element

// By Attribute
const input = document.querySelector('[name="email"]');  // Input with name="email"

// Complex Selectors (CSS ki tarah)
const nestedDiv = document.querySelector('div.container > p.highlight');
//                                       └────────┬────────┘
//                                                └─ CSS selector: exact match


// ────────────────────────────────────────────────────────────────────────────
// 2.2 querySelectorAll() - Sabhi Matches Return (NodeList)
// ────────────────────────────────────────────────────────────────────────────

// All elements with class 'item'
const allItems = document.querySelectorAll('.item');
//               └──────────┬──────────┘
//                          └─ Returns NodeList (array-like)

// Loop through NodeList
allItems.forEach((item, index) => {
    console.log(`Item ${index}:`, item);
});

// Convert NodeList to Array (agar array methods chahiye)
const itemsArray = Array.from(allItems);
// OR
const itemsArray2 = [...allItems];  // Spread operator


// ────────────────────────────────────────────────────────────────────────────
// 2.3 Old Methods (Compatibility Ke Liye)
// ────────────────────────────────────────────────────────────────────────────

// getElementById - Returns single element or null
const header = document.getElementById('header');  // No # needed

// getElementsByClassName - Returns HTMLCollection (live)
const buttons = document.getElementsByClassName('btn');  // No . needed
// Note: HTMLCollection = Live (DOM change hone pe update hota hai)

// getElementsByTagName - All elements with tag
const paragraphs = document.getElementsByTagName('p');

// HTMLCollection to Array conversion (forEach nahi chalega directly)
const paraArray = Array.from(paragraphs);
paraArray.forEach(para => console.log(para));


// ════════════════════════════════════════════════════════════════════════════
// SECTION 3: MODIFYING CONTENT (Text & HTML Change Karna)
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 📝 CONTENT MODIFICATION METHODS                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ textContent  - Plain text set/get (HTML tags render nahi hote)         │
│ innerHTML    - HTML as string set/get (tags render hote hain)          │
│ innerText    - Visible text only (CSS hide elements skip)              │
│                                                                          │
│ Best Practice:                                                           │
│ - textContent use karo for plain text (safe & fast)                    │
│ - innerHTML use karo for HTML content (XSS risk aware)                 │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 3.1 textContent - Plain Text (Safest & Fastest)
// ────────────────────────────────────────────────────────────────────────────

const title = document.querySelector('h1');

// GET - Current text nikalo
const currentText = title.textContent;
console.log(currentText);

// SET - New text set karo (HTML tags as plain text)
title.textContent = 'New Title';
title.textContent = '<strong>Bold</strong>';  // Tags render nahi honge (plain text)


// ────────────────────────────────────────────────────────────────────────────
// 3.2 innerHTML - HTML Content (Powerful but XSS Risk)
// ────────────────────────────────────────────────────────────────────────────

const container = document.querySelector('.container');

// GET - Current HTML nikalo
const currentHTML = container.innerHTML;
console.log(currentHTML);

// SET - New HTML set karo (tags render honge)
container.innerHTML = '<p>New <strong>paragraph</strong></p>';
//                     └────────────┬────────────┘
//                                  └─ HTML tags work karenge

// Multiple elements add karna
container.innerHTML = `
    <h2>Title</h2>
    <p>Paragraph 1</p>
    <p>Paragraph 2</p>
`;

// ⚠️ WARNING: User input directly innerHTML me mat dalo (XSS attack risk)
// const userInput = '<script>alert("Hacked!")</script>';
// container.innerHTML = userInput;  // ❌ DANGEROUS!

// ✅ SAFE WAY: User input ko sanitize karo ya textContent use karo
const userInput = '<script>alert("Safe")</script>';
container.textContent = userInput;  // Tags as plain text (safe)


// ────────────────────────────────────────────────────────────────────────────
// 3.3 innerText vs textContent (Difference)
// ────────────────────────────────────────────────────────────────────────────

const box = document.querySelector('.box');

// innerText - Sirf visible text (CSS hide elements skip)
console.log(box.innerText);     // Visible text only

// textContent - Sabhi text including hidden elements
console.log(box.textContent);   // All text nodes

// Performance: textContent faster hai (reflow trigger nahi karta)


// ════════════════════════════════════════════════════════════════════════════
// SECTION 4: MODIFYING STYLES (CSS JavaScript Se Change Karna)
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🎨 STYLE MODIFICATION                                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ Methods:                                                                 │
│ 1. element.style.property       - Inline styles (specific element)     │
│ 2. element.style.cssText        - Multiple styles at once              │
│ 3. element.setAttribute()       - Style attribute set                   │
│ 4. Classes modify                - Best practice (CSS file me styles)  │
│                                                                          │
│ ⚠️  Note: CSS property names camelCase me likhte hain JS me            │
│    CSS: background-color  →  JS: backgroundColor                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 4.1 Individual Style Properties (Inline Styles)
// ────────────────────────────────────────────────────────────────────────────

const box = document.querySelector('.box');

// Single property change
box.style.color = 'red';                    // Text color
box.style.backgroundColor = 'yellow';       // Background (camelCase!)
box.style.fontSize = '20px';                // Font size (unit zaroori)
box.style.padding = '10px';
box.style.margin = '20px auto';
box.style.border = '2px solid black';
box.style.borderRadius = '10px';

// GET computed style (actual rendered style)
const computedStyle = window.getComputedStyle(box);
console.log(computedStyle.color);           // Computed color value
console.log(computedStyle.fontSize);        // Computed font size


// ────────────────────────────────────────────────────────────────────────────
// 4.2 cssText - Multiple Styles At Once
// ────────────────────────────────────────────────────────────────────────────

// Multiple properties ek saath (CSS syntax)
box.style.cssText = 'color: blue; background: lightgray; padding: 15px;';
//  └─────┬─────┘
//        └─ CSS syntax me likho (semicolon separated)

// Existing styles overwrite ho jayengi (careful!)


// ────────────────────────────────────────────────────────────────────────────
// 4.3 setAttribute for Style (Not Recommended)
// ────────────────────────────────────────────────────────────────────────────

box.setAttribute('style', 'color: green; font-size: 18px;');
// ⚠️  Better to use .style property directly


// ════════════════════════════════════════════════════════════════════════════
// SECTION 5: WORKING WITH CLASSES (Best Practice for Styles)
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🎯 CLASS MANIPULATION (Modern & Best Way)                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ classList Methods:                                                       │
│ - .add('class')         - Class add karo                                │
│ - .remove('class')      - Class remove karo                             │
│ - .toggle('class')      - Add if absent, remove if present              │
│ - .contains('class')    - Check if class exists (true/false)            │
│ - .replace('old', 'new') - Old class ko new se replace                  │
│                                                                          │
│ ✅ Best Practice: Styles CSS file me likho, JS se classes toggle karo  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 5.1 classList.add() - Class Add Karna
// ────────────────────────────────────────────────────────────────────────────

const button = document.querySelector('.btn');

// Single class add
button.classList.add('active');
//     └────┬────┘ └─┬─┘
//          │       └─ Method
//          └───────── DOMTokenList (class list)

// Multiple classes add
button.classList.add('btn-primary', 'btn-large', 'shadow');


// ────────────────────────────────────────────────────────────────────────────
// 5.2 classList.remove() - Class Remove Karna
// ────────────────────────────────────────────────────────────────────────────

// Single class remove
button.classList.remove('active');

// Multiple classes remove
button.classList.remove('btn-large', 'shadow');


// ────────────────────────────────────────────────────────────────────────────
// 5.3 classList.toggle() - On/Off Switch (Most Useful!)
// ────────────────────────────────────────────────────────────────────────────

// Agar class hai to remove, nahi hai to add
button.classList.toggle('active');
//                       └──┬──┘
//                          └─ If exists → remove, else → add

// Return value: true (added) or false (removed)
const isActive = button.classList.toggle('active');
console.log(isActive);  // true if added, false if removed

// Use case: Show/Hide panels, Active buttons, Dark mode toggle
const panel = document.querySelector('.panel');
button.addEventListener('click', () => {
    panel.classList.toggle('hidden');  // Show/hide on click
});


// ────────────────────────────────────────────────────────────────────────────
// 5.4 classList.contains() - Check If Class Exists
// ────────────────────────────────────────────────────────────────────────────

// Returns true/false
if (button.classList.contains('active')) {
    console.log('Button is active');
} else {
    console.log('Button is inactive');
}


// ────────────────────────────────────────────────────────────────────────────
// 5.5 classList.replace() - Replace Old Class with New
// ────────────────────────────────────────────────────────────────────────────

button.classList.replace('btn-primary', 'btn-secondary');
//                       └─────┬─────┘  └──────┬──────┘
//                             │                └─ New class
//                             └──────────────────── Old class


// ────────────────────────────────────────────────────────────────────────────
// 5.6 Old Way: className (Not Recommended)
// ────────────────────────────────────────────────────────────────────────────

// className - All classes as string (overwrites existing)
button.className = 'btn btn-primary';  // ⚠️ Replaces all classes

// GET all classes
const classes = button.className;
console.log(classes);  // String: "btn btn-primary"

// ✅ Use classList instead (better & safer)


// ════════════════════════════════════════════════════════════════════════════
// SECTION 6: CREATING & REMOVING ELEMENTS
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🏗️  CREATING & MANIPULATING DOM ELEMENTS                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ Creating Elements:                                                       │
│ - document.createElement()      - Naya element banao                    │
│ - element.appendChild()         - Child ke end me add                   │
│ - element.prepend()             - Child ke start me add                 │
│ - element.insertBefore()        - Specific position pe add              │
│ - element.append()              - Multiple elements/text add            │
│                                                                          │
│ Removing Elements:                                                       │
│ - element.remove()              - Element ko remove (modern)            │
│ - parent.removeChild()          - Child remove (old way)                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 6.1 createElement() - Naya Element Banana
// ────────────────────────────────────────────────────────────────────────────

// Step 1: Element create karo
const newDiv = document.createElement('div');
//             └────────┬─────────┘ └──┬──┘
//                      │             └─ Tag name (string)
//                      └───────────────── Returns new element (memory me hai, page pe nahi)

// Step 2: Content add karo
newDiv.textContent = 'Hello, I am a new div!';

// Step 3: Styling/Classes add karo
newDiv.classList.add('box', 'highlight');
newDiv.style.padding = '10px';

// Step 4: Attributes add karo
newDiv.setAttribute('id', 'dynamic-box');
newDiv.setAttribute('data-info', 'created-by-js');

// Step 5: DOM me add karo (abhi tak sirf memory me hai)
document.body.appendChild(newDiv);  // Body ke end me add
//      └─┬─┘ └────┬────┘└──┬──┘
//        │        │       └─ Method: add as last child
//        │        └─────────── Parent element
//        └──────────────────── Root body element


// ────────────────────────────────────────────────────────────────────────────
// 6.2 appendChild() - Child Element Add Karna (End Me)
// ────────────────────────────────────────────────────────────────────────────

const container = document.querySelector('.container');
const paragraph = document.createElement('p');
paragraph.textContent = 'New paragraph';

// Add to container (as last child)
container.appendChild(paragraph);
//        └────┬────┘└────┬───┘
//             │          └─ Element to add
//             └──────────── Parent


// ────────────────────────────────────────────────────────────────────────────
// 6.3 prepend() - Child Element Add (Start Me)
// ────────────────────────────────────────────────────────────────────────────

const firstPara = document.createElement('p');
firstPara.textContent = 'I will be first!';

container.prepend(firstPara);  // Add as first child


// ────────────────────────────────────────────────────────────────────────────
// 6.4 append() - Multiple Elements/Text Add (Modern)
// ────────────────────────────────────────────────────────────────────────────

// Multiple elements aur text ek saath add
const span1 = document.createElement('span');
span1.textContent = 'Span 1';

const span2 = document.createElement('span');
span2.textContent = 'Span 2';

container.append(span1, ' - ', span2, ' - Done');
//        └──┬──┘└──┬─┘└──┬─┘└──┬──┘└──┬─┘└───┬───┘
//           │      │     │     │     │      └─ Plain text
//           │      │     │     │     └──────── Element 2
//           │      │     │     └────────────── Plain text
//           │      │     └──────────────────── Element 1
//           │      └────────────────────────── Plain text
//           └───────────────────────────────── Method

// Difference: appendChild() only elements, append() elements + text


// ────────────────────────────────────────────────────────────────────────────
// 6.5 insertBefore() - Specific Position Pe Add
// ────────────────────────────────────────────────────────────────────────────

const newElement = document.createElement('div');
newElement.textContent = 'Inserted before';

const referenceNode = document.querySelector('.reference');

// Insert before reference node
container.insertBefore(newElement, referenceNode);
//        └─────┬─────┘└─────┬────┘ └──────┬──────┘
//              │            │              └─ Reference (insert before this)
//              │            └──────────────── New element
//              └───────────────────────────── Method


// ────────────────────────────────────────────────────────────────────────────
// 6.6 insertAdjacentHTML() - HTML String Insert (Flexible)
// ────────────────────────────────────────────────────────────────────────────

const target = document.querySelector('.target');

// Position options:
// 'beforebegin' - Element ke pehle (as sibling)
// 'afterbegin'  - Element ke andar, first child ke pehle
// 'beforeend'   - Element ke andar, last child ke baad
// 'afterend'    - Element ke baad (as sibling)

target.insertAdjacentHTML('beforeend', '<p>New paragraph</p>');
//     └────────┬────────┘ └────┬────┘ └─────────┬─────────┘
//              │               │                 └─ HTML string
//              │               └───────────────────── Position
//              └───────────────────────────────────── Method


// ────────────────────────────────────────────────────────────────────────────
// 6.7 Removing Elements
// ────────────────────────────────────────────────────────────────────────────

// Modern way: .remove() (best!)
const elementToRemove = document.querySelector('.remove-me');
elementToRemove.remove();  // ✅ Simple & clean

// Old way: removeChild()
const parent = document.querySelector('.parent');
const child = document.querySelector('.child');
parent.removeChild(child);  // ⚠️ Parent reference chahiye

// Remove all children
container.innerHTML = '';  // ⚠️ Quick but loses event listeners

// Better way to remove all children
while (container.firstChild) {
    container.removeChild(container.firstChild);
}


// ────────────────────────────────────────────────────────────────────────────
// 6.8 Cloning Elements
// ────────────────────────────────────────────────────────────────────────────

const original = document.querySelector('.original');

// Shallow clone (no children)
const clone1 = original.cloneNode(false);

// Deep clone (with all children)
const clone2 = original.cloneNode(true);
//             └────┬────┘└────┬────┘
//                  │          └─ true = deep clone (children included)
//                  └──────────── Method

document.body.appendChild(clone2);


// ════════════════════════════════════════════════════════════════════════════
// SECTION 7: EVENT HANDLING (User Interactions)
// ════════════════════════════════════════════════════════════════════════════

/*
┌─────────────────────────────────────────────────────────────────────────┐
│ 🖱️  EVENT HANDLING (Most Important DOM Concept!)                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ Event = Koi action (click, typing, hover, etc.)                         │
│                                                                          │
│ Event Listener = Function jo event hone pe chale                        │
│                                                                          │
│ Common Events:                                                           │
│ - click        - Mouse click                                            │
│ - dblclick     - Double click                                           │
│ - mouseenter   - Mouse element pe aaye                                  │
│ - mouseleave   - Mouse element se jaaye                                 │
│ - input        - Input field me typing                                  │
│ - change       - Value change (dropdown, checkbox)                      │
│ - submit       - Form submit                                            │
│ - keydown      - Keyboard key press                                     │
│ - keyup        - Keyboard key release                                   │
│ - focus        - Element focused                                        │
│ - blur         - Element unfocused                                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
*/

// ────────────────────────────────────────────────────────────────────────────
// 7.1 addEventListener() - Event Listener Add Karna (Best Way!)
// ────────────────────────────────────────────────────────────────────────────

const button = document.querySelector('.btn');

// Basic syntax
button.addEventListener('click', function() {
    //                  └──┬──┘ └───────┬───────┘
    //                     │            └─ Callback function (runs on event)
    //                     └──────────────── Event name (string)
    
    console.log('Button clicked!');
});

// Arrow function (modern & clean)
button.addEventListener('click', () => {
    console.log('Button clicked with arrow function!');
});

// Named function (reusable)
function handleClick() {
    console.log('Handled by named function');
}
button.addEventListener('click', handleClick);


// ────────────────────────────────────────────────────────────────────────────
// 7.2 Event Object (Detailed Information)
// ────────────────────────────────────────────────────────────────────────────

button.addEventListener('click', (event) => {
    //                              └──┬──┘
    //                                 └─ Event object (automatically passed)
    
    console.log(event);                // Full event object
    console.log(event.type);           // 'click'
    console.log(event.target);         // Element that triggered event
    console.log(event.currentTarget);  // Element with listener (same as 'this')
    console.log(event.timeStamp);      // Time when event occurred
});


// ────────────────────────────────────────────────────────────────────────────
// 7.3 Mouse Events
// ────────────────────────────────────────────────────────────────────────────

const box = document.querySelector('.box');

// Click event
box.addEventListener('click', (e) => {
    console.log('Clicked at:', e.clientX, e.clientY);  // Mouse position
});

// Double click
box.addEventListener('dblclick', () => {
    console.log('Double clicked!